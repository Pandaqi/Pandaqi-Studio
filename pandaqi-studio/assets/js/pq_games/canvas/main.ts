import GridMapper from "./gridMapper"
import Color from "./color"

// @TODO: do we really need the main.ts files to summarize and provide everything? 
export default 
{
	gridMapper: GridMapper,
	color: Color
}