
export default (ctx:CanvasRenderingContext2D) =>
{
    /*ctx.globalAlpha = 1.0;
    ctx.filter = "none";
    ctx.globalCompositeOperation = "source-over";
    ctx.resetTransform();*/
    
    ctx.reset(); // this exists now!
}