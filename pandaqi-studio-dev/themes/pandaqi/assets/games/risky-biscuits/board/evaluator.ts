import BoardState from "./boardState";
import { CONFIG } from "./config"

export default class Evaluator
{
    isValid(board:BoardState) : boolean
    {

        // @TODO: check if there are at least 2 or 3 separate islands
        // @TODO: check if there is a good balance of ocean/land tiles

        return true;
    }
}