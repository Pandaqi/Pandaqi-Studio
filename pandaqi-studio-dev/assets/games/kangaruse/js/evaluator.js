export default class Evaluator
{
    constructor() {}
    isValid(board)
    {
        // @TODO: Ensure that score squares are distributed properly. (Count how many exist in each _quarter_ of the field. This number should be > 2 and not too far from the others.)
        // @TODO (optional): Find the _biggest_ connected group of holes. This may not be too large. (It means holes have clumped up.)
        // @TODO: Make sure starting positions (if enabled) are reasonably spread out
        return true; 
    }
}